# Normalise line endings so a checkout on Windows and one on Linux produce the
# same bytes, and therefore the same file fingerprints.
* text=auto eol=lf

*.png binary
*.jpg binary
*.gz  binary
*.zip binary
*.parquet binary

# Generated by the resolver; review it, but do not hand-merge it.
uv.lock linguist-generated=true merge=binary
