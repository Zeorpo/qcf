"""Project tooling.

A package rather than loose files so the modules can import each other by a
single unambiguous name, whether they are run as scripts or imported by the
test-suite. Nothing here is part of the ``qcf`` distribution.
"""
